export interface CustomerInfo {
  firstName: string;
  lastName: string;
  phoneNumber: number;
  email: string;
  city: string;
  zip: number;
  streetNumber: string;
  houseNumber: string;
  confirmConditions:boolean;
}
